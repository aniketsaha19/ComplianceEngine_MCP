name: Trigger auto deployment for mcp-gateway

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/mcp-gateway-AutoDeployTrigger-f599c0aa-4864-4d91-9dfb-844dc2ce0b70.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.MCPGATEWAY_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MCPGATEWAY_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MCPGATEWAY_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/mcp-server
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: rgcompliance1809.azurecr.io
          registryUsername: ${{ secrets.MCPGATEWAY_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MCPGATEWAY_REGISTRY_PASSWORD }}
          containerAppName: mcp-gateway
          resourceGroup: rg-compliance
          imageToBuild: rgcompliance1809.azurecr.io/mcp-gateway:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


